# Development Rules & Architecture Specification

## Working with AI Coding Agents (AIコーディングエージェントで作業する場合)
- **エラー自律修正ループ**: pythonスクリプトの実行やpytestでエラーが
  発生した際は、都度指示を待つのではなく「原因分析 -> コード修正 -> 再テスト
  実行」のループを、修正方針を変えながら最大5回まで自律的に回すこと。
  5回試みても解決しない場合は、無理に改悪を続けず、試した内容とエラー
  原因をまとめて報告すること。
- **厳格な完了判定**: 修正後は必ず実際にコマンド（`pytest` や
  `python translate_paper.py ...`）を実行すること。「エラーが出ずに
  コマンドが終了した」だけで成功とみなさず、指定の出力ファイル
  （`.md` / `.pdf`）が実際に生成され、中身が空になっていないかまで
  検証して成功と判定すること。
- **Git操作**: `git add` / `git commit` / `git push` / `git reset` 等は
  エージェント自身で実行しないこと。必要なgit操作は人間へコマンドとして
  提示すること。
- **操作スコープ**: ファイルの参照・編集・生成・コマンド実行は、すべて
  本プロジェクトのルートディレクトリ配下のみで行うこと。`.env` 等の
  秘密情報を含むファイルの内容出力・編集・削除は行わないこと。

## Project Overview
PDF学術論文・書籍の前処理・構造化解析および自動翻訳ツール。
PDFから本文、画像、メタデータを分離・抽出し、DeepL APIで翻訳した上で、1文ごとにユニークID（例: [P1-S1]）を付与したMarkdownおよびPDFを出力する。

## Architecture: 7-Stage Pipeline Model（本システムの前提とする工程モデル）
本システムは、`translate_paper.py`の`main()`を起点とする一本道のパイプラインを、以下の7工程（ページ範囲決定／PDF解析／構造化・タグ処理／翻訳実行／翻訳後処理／数式保護／PDF生成）に分解されるものとして構成する。この7工程モデルは`doc/architecture.md` §2「パイプラインの7工程」を正データとし、コード内のモジュール構成・docstringの説明・ディレクトリ分割・テストの分類（`testCode/`配下のテストファイル・クラス構成）など、本プロジェクトに関するあらゆる設計判断・議論・作業はこのモデルを前提として行うこと。
- **工程(1): ページ範囲の決定**: CLI引数（`--chapter`/`--start`/`--end`/`--start-label`/`--end-label`）から処理対象の物理ページ範囲を確定する。
- **工程(2): PDF解析**: 前半でMinerUをバックエンド（`pipeline`/`vlm-engine`）付きで実行し、`content_list.json`相当の生データを得る（7工程中、実行必須かつ唯一の重い外部処理）。後半でMinerU生出力を解析し、翻訳対象の本文テキストと非翻訳要素（数式・図表等）を分離してID体系を付与した上で、タグ付きMarkdown（`page_XX_en.md`）として書き出す（成果物結合）。
- **工程(3): 構造化・タグ処理**: タグ付きMarkdownから、翻訳直前のDocUnit・文脈（document_context）を組み立てる。
- **工程(4): 翻訳実行**: DeepL APIを実際に呼び出し、文脈付きで1文ずつ翻訳する。7工程中、工程(2)と並ぶ実行必須の工程。
- **工程(5): 翻訳後処理**: 数式プレースホルダを元の数式へ復元する（`apply_restore`）。
- **工程(6): 数式保護**: 未保護数式の検出と自動保護を行った上で、翻訳済みMarkdownを書き出す。
- **工程(7): PDF生成**: 翻訳済みタグ付きMarkdownから対訳版・英語版・日本語版のPDFをレンダリングする。

このうち工程(2)と工程(4)のみが実行必須（外部の重い処理を伴う）で、残る5工程（工程(1)(3)(5)(6)(7)）はいずれもMinerU・DeepLを呼ばずに独立してテスト可能である。各工程と担当関数・ファイルとの対応関係の詳細は`doc/architecture.md` §2「パイプラインの7工程」および §6、各`doc/architecture/<basename>.md`を参照すること。

## Key Requirements & Output Specifications
1. 不要領域の除外: ヘッダー（実行見出し・arXiv ID等）、フッター、ページ番号などの不要テキストを、MinerUが要素ごとに付与する種別（`header`/`footer`/`page_number`/`aside_text`）に基づいて除外する。
2. 画像の切り出し: 論文内の図表・画像を抽出し、output/images/ フォルダへPNG保存（例: fig_p1_1.png）。
3. 文単位の分割と結合:
   - 本文を「文（Sentence）」単位に分割。
   - ページをまたぐ未完成の文（跨ぎ文）を検出して適切に結合処理。
   - 行末のハイフネーション（例: de- + tection -> detection）を適切に結合。
4. 翻訳エンジン:
   - 翻訳にはDeepL API（要APIキー・実際に課金される）を使用する。APIキーはプロジェクトルート直下の `.env`（`DEEPL_API_KEY`、git管理対象外）から読む。無料プランのキーでも動作するが、文字数消費に応じて課金対象になる。
   - 各文の翻訳リクエストに、文書全体の要約と直前2〜3文の原文を `context` パラメータとして渡し、代名詞や専門用語の表記揺れを抑える。文単位で逐次送信する。
   - APIキー未設定・文字数上限到達・通信エラー等は `TranslationBackendError` を経て `SystemExit` となり処理を止める。
   - 実DeepL実行時の課金・許可運用は「Test & Execution Rules」項目3を参照。無料の代替翻訳経路は無いため、日常の開発ループではpytest（`call_deepl`をモック化）を用いる。
5. ページ範囲指定（長大PDF/書籍対策）:
   - 物理ページ指定: `--start` および `--end` 引数（数値）で絶対インデックス範囲を指定。
   - 印刷ページラベル指定: `--start-label` および `--end-label` 引数で、PDF内の印刷表記（"cov", "i"〜"xviii", "36" 等）による範囲を指定可能にする。物理ページ番号との相互互換性を維持すること。
   - 指定されたページ範囲のみを抽出し、高速・安全に処理を完了させること。
6. ナンバリング（ID付与）と出力:
   - 各文に `[P{page_num}-S{sentence_num}]` のID（物理ページ番号ベース）を付与。
   - ページごとのMarkdownファイル（例: output/page_01_en.md）および最終結合PDFを出力。

## Test & Execution Rules (テストおよび実行運用規定)
1. **テスト・実行対象データ**:
   - 軽量テスト用: `input/sample0.pdf`
   - 表抽出・章解決・印刷ページラベル確認用: `input/sample1.pdf` / `input/sample2.pdf`（論文フルサイズ。
     いずれもarXivのバージョン固定URL(`v1`)から取得しており、著者の将来の
     改訂でテストのハードコード期待値が崩れないようにしている）。
     **`sample2.pdf`は数式保護・翻訳品質の保証対象外とする**（現行の
     計算機資源では`sample2.pdf`をMinerUバックエンド`vlm-engine`で
     十分に実行できないため）。`sample2.pdf`で確認されているMinerU側の
     数式誤検出・境界検出の乱れ（単体大文字の冠詞を数式変数と誤認する等）
     は、`sample2.pdf`固有の問題ではなく、`pipeline`バックエンド
     （項目3参照。`vlm-engine`より認識精度が低い代わりに高速なバック
     エンド）の精度限界に起因すると考えられる。**現在の実DeepL許可
     組み合わせのうち3件（`sample0`・`sample1`・`sample3_label55-60`）
     も同じ`pipeline`バックエンドを使っているため、これらでも理論上は
     同種の誤検出が起こりうる**（これまでの目視確認では顕在化して
     いないだけの可能性がある）。`vlm-engine`で実行すれば解消される
     可能性が高いという見立てだが未検証の作業仮説であり、「sample2固有
     の問題だから対象外にした」という理解はしないこと。
   - 実稼働・章単位テスト用: `input/sample3.pdf`（**ただし、指定ページ範囲 `--start` / `--end` または `--start-label` / `--end-label` のみ指定して実行すること**）
2. **`sample3.pdf` 運用ルール**:
   - `sample3.pdf` 全体を一括で処理・テストすることは実行時間およびDeepL APIコスト管理の観点から禁止する。
   - `sample3.pdf` を対象とする場合は、DeepL APIの文字数消費を抑えるため、指定した章・ページ範囲のみを最小限で処理対象とすること。
3. **テスト実行指示・翻訳エンジンのモック化**:
   - 通常の開発ループでは `pytest` または `sample0.pdf` を用いた高速な動作確認を中心に行い、章・範囲単位の確認が必要な場合のみ `sample3.pdf` の指定範囲を実行すること。
   - **`pytest` 実行時のテスト翻訳では、DeepL APIへ実際に通信しないよう、翻訳呼び出し関数 `call_deepl` をモック化して自動化すること。** 翻訳経路はDeepL（有料）のみであり、無料の代替経路は無い。
   - **例外**: 実際のDeepL翻訳結果でしか検証できない項目（例: `page_XX_ja.md`の生成・未検出の数式らしき候補の検出が、実際のDeepL翻訳結果に対しても正しく機能するかの確認）に限り、DeepLを実課金でモック化せずに呼び出すテストを許可する。**許可される組み合わせは下表の4件のみ**（`test_run_pipeline_end_to_end_with_real_deepl`のparametrizeケース）であり、表に無い組み合わせ（対象PDF・範囲・MinerUバックエンドのいずれの軸でも）は許可しない。翻訳エンジンモック前提のテスト（`test_translate_and_export_with_mocked_translation`等）にはこの例外自体が適用されない。

     | テストID（`-k`指定用） | 対象PDF | 範囲 | MinerUバックエンド | 備考 |
     | --- | --- | --- | --- | --- |
     | `sample0_full_pipeline_deepl` | `sample0.pdf` | 全体（2ページ） | `pipeline`（既定） | 日常の開発ループでは実行しない基本ケース |
     | `sample1_full_pipeline_deepl` | `sample1.pdf` | 全体（9ページ） | `pipeline`（既定） | sample0.pdfの内容を包含するスーパーセット。sample0版より高コストなため`-k`で明示指定時のみ実行 |
     | `sample3_label55-60_pipeline_deepl` | `sample3.pdf` | 印刷ページラベル55〜60＝物理67〜72（6ページ） | `pipeline`（既定） | `sample3.pdf`は239ページの大型書籍であり、項目2の全体一括処理禁止規定に従い最小限の範囲に限定 |
     | `sample3_label56-56_vlm-engine_deepl` | `sample3.pdf` | 印刷ページラベル56のみ＝物理68（1ページ） | `vlm-engine` | `vlm-engine`バックエンドでも翻訳・数式保護・PDF生成まで一連のパイプラインが問題なく通ることを確認する専用ケース。範囲を1ページに絞り課金を最小化 |

     いずれも日常の開発ループでは実行せず、`-k`で明示的に指定した場合のみ実行すること。testIDの末尾`_deepl`は、実際に課金される実DeepLテストであることを`-k`指定時に明示するためのサフィックス。

     **許可されない組み合わせの例**（上表に無いもの。テストコード上もparametrizeされていない）:
     - `sample2.pdf`での実DeepL実行（範囲・バックエンド問わず）
     - `sample3.pdf`の上記2範囲（label55-60・label56）以外の範囲での実DeepL実行
     - `sample0.pdf`・`sample1.pdf`を`vlm-engine`バックエンドで実行するケース（未実装。追加する場合は本表の更新とあわせて行うこと）
   - **実DeepL課金テストの実行前確認（AIによる自律実行の禁止）**: 上記の例外に該当する実DeepLテスト（上表の4ケース等、名称に`real_deepl`を含むテストや、それに準ずる実課金コマンド）は、`.env`にDEEPL_API_KEYが設定済みであっても、AIコーディングエージェントが自らの判断で実行してはならない。実行する前に必ず人間へその旨を伝え、明示的な許可を得ること。フルスイート実行（`pytest`単体実行）はこれらを`pytest.ini`の`addopts = -m "not real_deepl"`で自動的に除外するが、実DeepLテストを対象に含める場合は個別に許可を取った上で`-k`で明示的に指定すること。
4. **未保護インライン数式のフルチェック（許可リストの運用）**:
   - 「実際にDeepLを呼んでcache/配下へ結果を記録すること」と「その結果に対して許可リスト突合を行うこと」は別のテストに分かれている。前者は`test_run_pipeline_end_to_end_with_real_deepl`（項目3の許可された4組み合わせに限定）が担い、実行するたびに実際に課金される。後者は`test_untranslated_fragment_candidates_against_cached_deepl_output`（`testCode/test_integration.py`）が担い、cache/配下に残っている最新の記録を読むだけでDeepLは一切呼ばないため無課金であり、日常の開発ループで気軽に実行してよい（対応する記録が無い場合はスキップされる）。**この許可リスト突合は実DeepL許可4組み合わせ全て（sample0/sample1/sample3_label55-60「`pipeline`バックエンド」/sample3_label56-56「`vlm-engine`バックエンド」）を対象とし、sample3_label55-60とsample3_label56-56は同じ書籍（sample3.pdf）の許可リストKNOWN_FALSE_POSITIVE_FRAGMENTS_SAMPLE3・KNOWN_LEAKED_MATH_FRAGMENTS_SAMPLE3を共用する**（新たな候補が出た場合は、本項目の運用（人間による目視確認の上での許可リスト追加）に従うこと）。
   - `test_untranslated_fragment_candidates_against_cached_deepl_output`は、記録された実際のDeepL翻訳結果から`mainCode/stage6/stage6.py`の`find_untranslated_fragment_candidates`で未保護の数式らしき候補を洗い出し、`KNOWN_FALSE_POSITIVE_FRAGMENTS`（固有名詞・略語・列挙記号）／`KNOWN_LEAKED_MATH_FRAGMENTS`（本物の未保護数式）という2つの許可リストに無い未知の候補が出たら失敗する回帰テストである。
   - DeepLの翻訳結果は完全に決定的ではないため、言い回しの変化により、記録が更新されるたびにこのテストが失敗することがある。失敗時は人間が実際の`page_XX_ja.md`・原文PDFを目視確認し、新しい候補を上記いずれかの許可リストへ追加すること。**内容を確認せず機械的にリストへ追加してテストを通すことは禁止する**（本テストの目的（未保護の数式漏れの検知）を損なうため）。目視確認する`page_XX_ja.md`は、`protect_confirmed_single_letter_leaks`適用**後**の最終版（`output_dir`側。存在する場合）を優先する。`cache/.../05_restored/page_XX_ja.md`は同適用**前**のスナップショットのため、単体アルファベット数式変数の保護有無が異なる点に注意。
   - 本物の数式漏れ（`KNOWN_LEAKED_MATH_FRAGMENTS`）は、可能な限り許可リストへの追加ではなく`mainCode/stage2/stage2.py`（`wrap_bare_greek_letters`・`wrap_bare_letter_equals_expressions`）・`mainCode/stage6/stage6.py`（`protect_confirmed_single_letter_leaks`・`merge_comparison_math_spans`等）側での自動`$...$`保護の実装を優先すること。ギリシャ文字（`wrap_bare_greek_letters`）、「1文字の変数 = 値」形式の数式的表現（例:"t = 1"、`wrap_bare_letter_equals_expressions`）、翻訳後も半角のまま残る単体アルファベット変数（`protect_confirmed_single_letter_leaks`）、既存の`$...$`スパン直後に比較演算子付きの断片が地の文で続く表現（例:"$K$ > 1"、`merge_comparison_math_spans`）は既にこの方針で自動保護済みであり、許可リストへの追加は「実在の固有名詞・略語と自動判別できない場合（例:"DiT","NMS"）」「英字を含まない裸の数字1文字等、パターンとして機械的に切り出せない場合」の最終手段として運用する。
5. **実データ依存テストと汎用ロジックテストの分離**:
   - `resolve_chapter_page_range`（章番号→ページ範囲）や`resolve_physical_page`（印刷ページラベル→物理ページ番号）のように、特定の論文・書籍の内容には依存しないはずの汎用アルゴリズムを検証する場合、ダウンロードした実サンプルPDF（`sample1`〜`sample3.pdf`）の実際の目次・ページラベル構成に期待値を直接ハードコードしてはならない。実PDFの内容変化（著者の改訂、当初の期待値の転記ミス等）でコードは正しいままテストだけが壊れるリスクがあるため（実際に本プロジェクトで発生した）。
   - 代わりに、`fitz`（PyMuPDF）でテスト実行時に組み立てる目次・印刷ページラベルのみを持つ使い捨ての合成PDF（`testCode/test_stage1.py`の`_build_synthetic_toc_pdf`/`_build_synthetic_labeled_pdf`参照）を使い、アルゴリズムの性質だけを決定的に検証すること。
   - ただし「実際の論文・書籍PDFでMinerU/fitzが正しく読み取れているか」という統合的な確認自体は不要にはならないため、各アルゴリズムにつき最低1テストは意図的に実データ（`sample2.pdf`/`sample3.pdf`）のまま残し、全件を合成データに置き換えないこと。
6. **MinerU実行結果のローカルキャッシュ運用**:
   - MinerUの実行結果（`content_list.json`相当のデータおよび抽出画像）は、著作権上の理由から`input/`・`output/`と同様にgit管理対象外とする（`cache/`、`.gitignore`で除外済み）。手動編集・コミットは禁止する。
   - 各キャッシュフォルダには、MinerU由来の実データ（`content_list.json`・`images/`）に加え、このプロジェクトが添える`meta.json`（キャッシュ生成条件の指紋。MinerUの出力ではない）が入る。`meta.json`が記録するのは対象PDFファイルのSHA-256ハッシュ・ページ範囲・MinerUバージョン・バックエンド・キャッシュ形式バージョンで、読み込み時にこれらが現在の要求と全一致したときだけキャッシュを再利用し、1つでも変化していれば自動的に無効化して再実行する。手動でのキャッシュ破棄・鮮度管理は不要。
   - キャッシュ機構（`mainCode/stage2/stage2.py`のキャッシュ節。`load_cached_items`/`save_cache`/`get_mineru_version`等）は速度最適化のみを目的とし、読み込み・保存いずれの失敗時も必ず通常のMinerU実行にフォールバックする。挙動を疑う場合は環境変数`MINERU_CACHE_DISABLE=1`で無効化して実行できる。
   - MinerUバージョン更新時は自動的に実データでの再実行が強制されるため、項目5の「実データを用いた最低1テスト」の要件と矛盾しない。ディスク容量が気になる場合は`cache/`ごと削除してよい（次回実行時に自動再生成される）。
   - `cache/<PDFファイル名>_<範囲記述子>/`配下には、MinerUキャッシュ（`mineru_cache/`）のきょうだいとして、実際にDeepLを呼んだ際の送受信内容が`real_deepl_output_<実行日時>/`にも記録される（`mainCode/whole_pipeline/whole_pipeline.py`の`main`が備える機能で、人間による本実行と、テストの中では全体テストの実DeepL版`test_run_pipeline_end_to_end_with_real_deepl`から呼ばれた場合に生成する。他のテストは実際のDeepLを呼ばないため生成しない）。こちらも`cache/`と同様にgit管理対象外・手動編集禁止。実行のたびにタイムスタンプ付きの新しいフォルダに書き込まれるため過去の記録は上書きされない。
7. **開発ループでの実行範囲の限定**:
   - 変更が特定のモジュール・機能に閉じている場合は、フルテストスイート（`pytest`単体実行）ではなく、関連するテストファイル・テスト関数のみを`pytest <file>`や`-k <pattern>`で限定して実行すること。
   - フルスイートは`pytest.ini`の`addopts = -m "not real_deepl"`により実DeepL課金テストを自動的に除外するが、変更の影響範囲を見極め、必要最小限のテストのみを実行すること。
8. **テストファイルの構成方針（メインコードの工程・モジュール構成とのミラーリング）**:
   - テストファイルは、メインコードの工程別フォルダ構成（`stage1/`〜`stage7/`）をミラーする形で、工程ごとに1ファイル＋複数工程を横断するパイプライン全体・統合テスト用ファイル＋`shared.py`専用ファイルの合計10ファイルに分割する。ファイル数が増えるため、これら10ファイルはメインコードのように工程別サブフォルダへさらに分けず、`testCode/`フォルダ配下に1階層でまとめて格納する。
   - あるテストクラスをどの工程ファイルに置くかは、そのテストクラスが検証している対象コードが実際にどの工程の役割を果たしているかで決める。対象コードが定義されているソースファイルの置き場所（特定の工程フォルダかルートか）ではなく、検証している関数・処理が担う役割で判定すること。工程(2)専用の中間表現（`TextBlockElement`等）や工程(7)専用の表示単位（`Block`）のように、単一の工程でしか使われない型は、この判定基準に従い`mainCode/shared/shared.py`ではなくその工程のモジュール（`stage2.py`・`stage7.py`）側に定義し、対応する工程別テストファイルで検証する。
   - `mainCode/shared/shared.py`（複数工程から共通して呼ばれるデータ構造＋`protect`/`restore`）は特定の工程フォルダに属さずルートに置かれており、対応する専用テストファイル`testCode/test_shared.py`を持つ。工程別テストを通じた間接的な検証だけに頼ると、どの工程の通常データからも踏まれない`shared.py`自身の防御的な分岐・境界条件（例: `restore`のプレースホルダ番号が範囲外・想定外に重複出現した場合の挙動）が見落とされることが実際に確認されたため、`shared.py`の入出力契約（事前条件・事後条件・エッジケース）を直接対象とする専用ファイルを設ける。ただし、特定の工程の役割として検証すべき振る舞い（`protect_units`・`normalize`・`apply_restore`等、各stageのラッパー関数自体の挙動や、実データを使った統合的な確認）は引き続き該当する工程別テストファイルに置き、`test_shared.py`とは内容を重複させないこと。
   - 複数工程・複数モジュールを跨いで呼び出す一連の振る舞い（パイプライン全体・翻訳実行と後処理の連携等）を検証するテストは、いずれか1つの工程ファイルにも属さないため、統合/全体テスト用ファイルに置くこと。
   - 実際の構成は`testCode/`フォルダ配下の10ファイル（`test_shared.py`・`test_stage1.py`〜`test_stage7.py`、`test_whole_pipeline.py`、`test_integration.py`）＋`conftest.py`（複数ファイルで共有するフィクスチャ・定数・ヘルパーのみを置く）である。
   - 1つの関数が検証している「一般的な仕様」と「実データで見つかった特定の境界条件・不具合の再現（docstringに「実データ回帰」等と明記）」は、同じクラス内に同居させ、別クラス・別ファイルへ物理分離しないこと。回帰テストはその関数の一般則に対する追加の境界条件という位置づけであり、近くに置くことで「この関数が考慮している境界条件の全体像」が一目でわかる（分離すると一般テストだけを見た読者がその関数の全境界条件を把握できなくなり、かえって可読性が落ちる）。

## Code Standards
- Python 3.13+
- テストは pytest を使用。
- 型ヒント（Type Hints）の徹底。

## Source Layout & Architecture-Doc Rules (メインコード構成・設計ドキュメント規則)
`mainCode/`（9ファイル: `stageN/stageN.py` ×7 ＋ `shared/shared.py` ＋ `whole_pipeline/whole_pipeline.py`）と `doc/architecture/<basename>.md` は1対1対応。既存ファイルを範型とし、様式を崩さないこと。

- **`.py`**: 先頭にモジュールdocstring（そのファイルの役割＋処理の一言要約のみ。グループの内訳は列挙せず `# ===` バナーと `doc/architecture/<basename>.md` に委ね、末尾に「構成の詳細は `doc/architecture/<basename>.md` を参照」と添える。`whole_pipeline.py` の7工程の番号付きリストのようなパイプライン全体の地図は可）。関数・クラスの説明は `def`/`class` 直前の `#` コメントに書く（関数docstringは使わない。`"""..."""` はモジュールdocstringのみ）。入口関数の本体には番号付きステップコメント（`# 1.` `# 2.` …）。ファイル内は `# `＋`=`×76 のバナーで概念グループに区切り、概ねボトムアップ順（末端ヘルパー・型が上）に並べて工程の入口グループを最後に置く。単一工程専用の型（工程(2)の `TextBlockElement`、工程(7)の `Block` 等）はその工程のモジュールに、複数工程共有のもののみ `shared.py` に置く。
- **`.md`**: 5節構成（§1概要〈役割＋「やっていることを一言でいうと〜」の要約段落＋グループ箇条書き〉／§2構成要素（Nグループ）／§3 `<入口>()`の処理フロー〈§3.1依存関係図＋設計・エラー処理の小節〉／§4構成要素リファレンス／§5関連ドキュメント）。**§2グループ ↔ `.py` の `# ===` バナーは1対1**（片方を変えたら §2・§3.1図・§4のグループタグを同時に追随）。**§3.1図にはファイル内の全関数を登場させ（複数箇所から呼ばれる関数は初出でサブツリー展開）、§4はその図順で関数・ヘルパークラス、末尾にデータ構造を漏れなく記載**。**§4の各項目は先頭に `種別`（`関数`／`データ構造`（受け身のdataclass）／`ヘルパークラス`（可変状態＋メソッドを持つ素のクラス））を持ち、種別ごとに書式が異なる**（関数＝`stage3.md`、データ構造＝`stage7.md`の型項目、ヘルパークラス＝`stage2.md`の`_PageBuilder`〈種別／グループ／役割／状態(表)／メソッド(表)／ライフサイクル〈生成・更新・消費を1段下げた入れ子〉／テスト対象〉に倣う）。**§2の各グループ名の末尾に `（種別）` を付ける**（そのグループの§4項目が持つ種別の集合。全関数グループも `（関数）`、混在は `・` で並記。素の名前だと曖昧なグループは語義補足を `〔　〕` に入れてから `（種別）`）。§1の箇条書きは機能別の粗い概観であり、`（種別）` は付けず、§2グループと1対1である必要もない（正データは§2）。§4各項目の `- **グループ**:` 行と §3.1図の注記は `（種別）` を付けない短い名前を使う。`pass` のみの例外クラス・モジュール定数は項目を作らず、`（種別）`にも数えず、送出関数の項や §2説明文で言及。`.py` 側の `# ===` バナーには `（種別）` を付けない。テーブルのセル内 `|` は `\|` でエスケープ。
- **例外**: `stage5.py`（関数1つ）はバナー・§3.1図・§4並び規則の対象外。`shared.md` は入口が無いため §3 が「呼び出し関係」・§4 は §2 グループ順。`shared.md`・`whole_pipeline.md` の §1 に要約段落は無い。

## Documentation & Comment Style Rules (文書・コメント記述規則)
- README.md、`doc/`配下のドキュメント、コード内のコメント・docstringを含む、ファイル上のあらゆる自然言語記述は、無時間的な表現で書くこと。「これまで」「従来」「過去に」「以前は」「新たに」「今回」「初めて」「経緯」等、前のバージョン・変更履歴の存在を前提にした書き方をしないこと。日時・タイムスタンプも本文中に含めないこと。その変更履歴を知らない読者にも、現在の仕様としてそのまま理解できる記述にすること。
- 例外: テストの実行履歴・ログ・キャッシュのタイムスタンプ等、実行履歴そのものを記録する目的のデータ・出力は対象外とする（例: `cache/`配下の`real_deepl_output_<実行日時>/`フォルダ名、pytest実行ログの内容等）。
