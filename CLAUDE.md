# Development Rules & Architecture Specification

## Human Operational Rules (人間側の起動設定)
- Claude Code 起動コマンド:
  claude --dangerously-skip-permissions

## Loop Engineering & Execution Rules (自律ループ動作ルール)
1. エラー自律修正ループの徹底:
   - python スクリプトの実行や pytest でエラーが発生した際、人間に報告して指示を待つのではなく、原因分析 -> コード修正 -> 再テスト実行 のループを修正が完了するまで自律的に回すこと。
   - 1つのエラーに対して修正方針を変えながら最大5回まで自律トライすること。
   - 5回修正を試みてもテストが通らない場合は、無理に改悪を続けず、試した内容とエラー原因をまとめて人間に報告し指示をあおぐこと。
2. テスト駆動・ログベースの検証（厳格な完了判定）:
   - 修正後は必ず自らターミナルで実行コマンド（pytest や python translate_paper.py ...）を叩くこと。
   - 単に「エラーが出ずにコマンドが終了した」だけで成功とみなさず、指定された出力ファイル（.md や .pdf）が実際に生成されているか、中身が空になっていないかまで自律検証して成功と判定すること。
3. Git 操作の絶対禁止:
   - git コマンド（git add, git commit, git push, git reset 等）は絶対にAI自身で実行しないこと。必要な Git 操作がある場合は人間へコマンドを提示すること。
4. 操作スコープ制限:
   - 操作（ファイルの参照・編集・生成・コマンド実行）は、すべて本プロジェクトのルートディレクトリ配下のみで行うこと。
   - .env や秘密情報が含まれるファイルの内容出力、編集、削除は禁止する。

## Project Overview
PDF学術論文・書籍の前処理・構造化解析および自動翻訳ツール。
PDFから本文、画像、メタデータを分離・抽出・翻訳（DeepL）し、1文ごとにユニークID（例: [P1-S1]）を付与したMarkdownおよびPDFを出力する。

## Key Requirements & Output Specifications
1. 不要領域の除外: ヘッダー（arXiv ID等）、フッター、ページ番号などの不要テキストを座標（y座標判定）で除外。
2. 画像の切り出し: 論文内の図表・画像を抽出し、output/images/ フォルダへPNG保存（例: fig_p1_1.png）。
3. 文単位の分割と結合:
   - 本文を「文（Sentence）」単位に分割。
   - ページをまたぐ未完成の文（跨ぎ文）を検出して適切に結合処理。
   - 行末のハイフネーション（例: de- + tection -> detection）を適切に結合。
4. 翻訳エンジン:
   - DeepL API のみを使用して翻訳する。
5. ページ範囲指定（長大PDF/書籍対策）:
   - 物理ページ指定: `--start` および `--end` 引数（数値）で絶対インデックス範囲を指定。
   - 印刷ページラベル指定: `--start-label` および `--end-label` 引数で、PDF内の印刷表記（"cov", "i"?"xviii", "36" 等）による範囲を指定可能にする。物理ページ番号との相互互換性を維持すること。
   - 指定されたページ範囲のみを抽出し、高速・安全に処理を完了させること。
6. ナンバリング（ID付与）と出力:
   - 各文に `[P{page_num}-S{sentence_num}]` のID（物理ページ番号ベース）を付与。
   - ページごとのMarkdownファイル（例: output/page_01_en.md）および最終結合PDFを出力。

## Test & Execution Rules (テストおよび実行運用規定)
1. **テスト・実行対象データ**:
   - 軽量テスト用: `input/sample0.pdf`
   - 実稼働・章単位テスト用: `input/sample3.pdf`（**ただし、指定ページ範囲 `--start` / `--end` または `--start-label` / `--end-label` のみ指定して実行すること**）
2. **`sample3.pdf` 運用ルール**:
   - `sample3.pdf` 全体を一括で処理・テストすることは実行時間およびDeepL APIコスト管理の観点から禁止する。
   - `sample3.pdf` を対象とする場合は、DeepL APIの文字数消費を抑えるため、指定した章・ページ範囲のみを最小限で処理対象とすること。
3. **テスト実行指示・デフォルト翻訳エンジン**:
   - 通常の開発ループでは `pytest` または `sample0.pdf` を用いた高速な動作確認を中心に行い、章・範囲単位の確認が必要な場合のみ `sample3.pdf` の指定範囲を実行すること。
   - **`pytest` 実行時のテスト翻訳では、DeepLの有料APIキーを消費しないよう、DeepL呼び出し自体をモック化して自動化すること。**

## Code Standards
- Python 3.13+
- テストは pytest を使用。
- 型ヒント（Type Hints）の徹底。